import 'package:flutter/material.dart';

class GetBudgetBookingStep extends StatelessWidget {
  const GetBudgetBookingStep({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Get budget'),
    );
  }
}
