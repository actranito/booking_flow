import 'package:flutter/material.dart';

class GetUserNameBookingStep extends StatelessWidget {
  const GetUserNameBookingStep({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Get User\'s name'),
    );
  }
}
